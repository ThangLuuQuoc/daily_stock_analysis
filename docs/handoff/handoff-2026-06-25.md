# Handoff — daily_stock_analysis × OpenStock (thị trường VN)

Ngày: 2026-06-25 · Workspace: `F:\2026\2027\dailysignals\daily_stock_analysis`
OpenStock (nguồn dữ liệu VN, Bun/Elysia): `F:\2026\2027\openstockV1` — **không sửa**, chỉ dùng endpoint public.

## Mục tiêu phiên
Tích hợp `daily_stock_analysis` (hệ phân tích cổ phiếu bằng LLM, FastAPI + React) để dùng OpenStock làm nguồn dữ liệu thị trường Việt Nam, chạy được dashboard, và Việt hoá giao diện.

---

## Đã hoàn thành (theo thứ tự)

1. **OpenStock adapter (Phase 0 MVP)** — xem chi tiết + gaps tại [plan/openstock_adapter_gaps.md](F:\2026\2027\dailysignals\daily_stock_analysis\plan\openstock_adapter_gaps.md) và memory `openstock-adapter.md`.
   - `data_provider/openstock_fetcher.py`, `openstock_fundamental_adapter.py`, `openstock_symbols.py`.
   - **Đã wire vào `DataFetcherManager`**: `_is_vn_market()` (gate theo `openstock_enabled`, ưu tiên trước US) + nhánh "vn" trong `get_daily_data` và `get_realtime_quote` (`data_provider/base.py`). Import fetcher TQ/US làm defensive (`__init__.py` + `_safe_instantiate`) để boot không cần akshare/efinance.
   - Smoke test: `scripts/test_openstock_adapter.py`; server test nhẹ: `scripts/openstock_adapter_server.py`.

2. **Backend bootable + chạy** — cài deps (trừ China data sources). `.env` tối thiểu đã tạo.

3. **i18n Tiếng Việt cho UI** — `apps/dsa-web/src/i18n/uiText.ts` (673 keys × zh/en/vi), `featureText.ts` (28 map), wiring (`uiLanguage.ts`, `UiLanguageContext.tsx`, `UiLanguageToggle.tsx` cycle 3 ngôn ngữ). Sidebar nới rộng `136→184px` để hết tràn chữ.

4. **Search cổ phiếu** — chuyển từ index tĩnh CN sang **live OpenStock**: backend `GET /api/v1/stocks/search` (proxy OpenStock `/search`) + frontend `searchStocksRemote` + option `remoteSearch` trong `useAutocomplete`.

5. **LLM** — OpenAI, model **`gpt-5.4-mini`** (lưu ý: `gpt-5.5-mini` KHÔNG tồn tại trên tài khoản). Key trong `.env` (đã redact ở đây). Pricing: $0.75/1M input, $4.50/1M output → ~$0.013/lần phân tích (~5k in + 2k out).

6. **Token tracking** — đã có sẵn: trang "Mức sử dụng" + `GET /api/v1/usage/dashboard` (per-call, kèm mã CK).

7. **Việt hoá progress phân tích** — 11 chuỗi `_emit_progress` ở `src/core/pipeline.py` + 3 chuỗi ở `src/analyzer.py:3042,3170,3218` đã dịch VN.

8. **Report language theo UI** — `HomePage.tsx` analyze/reanalyze gửi `report_language` theo UI (`uiToReportLanguage`: en→en, vi→en, zh→zh).

9. **Audit i18n tổng thể + tracking** — [plan/i18n_tracking.md](F:\2026\2027\dailysignals\daily_stock_analysis\plan\i18n_tracking.md) (kiến trúc, ~85 chuỗi FE + ~80 chuỗi BE còn tiếng Trung, ưu tiên P0–P3).

---

## Trạng thái chạy hiện tại

| Service | Port | Khởi động |
|---|---|---|
| OpenStock (data VN) | 3000 | của user, sẵn chạy — KHÔNG tắt |
| DSA backend (FastAPI) | 8000 | `OPENBLAS_NUM_THREADS=1 OMP_NUM_THREADS=1 PYTHONIOENCODING=utf-8 python -m uvicorn server:app --host 127.0.0.1 --port 8000` |
| dsa-web (Vite) | 5174 | `cd apps/dsa-web && bunx vite --port 5174 --host 127.0.0.1` (5173 bị OpenStock chiếm) |

Lưu ý môi trường:
- **Python 3.14**, numpy cần `OPENBLAS_NUM_THREADS=1 OMP_NUM_THREADS=1` (nếu không → OpenBLAS OOM). Console cp1252 → `PYTHONIOENCODING=utf-8`.
- **bun install** cần `bunfig.toml` trỏ corp CA (`apps/dsa-web/bunfig.toml` đã tạo, trỏ `C:/Program Files/Git/mingw64/ssl/certs/ca-bundle.crt`).
- China data libs (akshare/efinance/tushare...) **không cài** — defensive import bỏ qua.
- Background process KHÔNG sống qua các turn — phải start lại mỗi phiên.

---

## Việc còn lại (ưu tiên — chi tiết file:line trong plan/i18n_tracking.md)

- **P0**: R1 — nhãn báo cáo bám theo `selectedReport.meta.reportLanguage`, không theo UI (`HomePage.tsx:233`) → đổi English vẫn ra nhãn Trung trên báo cáo cũ. Tên CK fallback `股票{code}` (`stock_service.py:175`, `pipeline.py:440`, `analyzer.py`). Lỗi API tiếng Trung (`error_handler.py`, `endpoints/analysis.py`, `endpoints/stocks.py`).
- **P1**: ~85 chuỗi hardcode FE (alerts/portfolio/autocomplete/suggestions); thông báo/email tiếng Trung (`notification.py`, `notification_sender/*`).
- **P2**: ternary `=== 'zh'` không scale; date locale khoá `zh-CN` (`format.ts:6,20`); dọn default-to-zh.
- **P3**: thêm `vi` cho NỘI DUNG báo cáo (BE `report_language.py` chỉ zh/en; prompt `analyzer.py:3718/3730`, `market_analyzer.py:1249/1313`).
- **Adapter gaps** (plan/openstock_adapter_gaps.md): `/stocks/:symbol/financials` trả rỗng (DB chưa seed BCTC → YoY/financial_report=None); thiếu endpoint dividends & ownership; news sẽ lấy từ OpenStock sau (thay newspaper3k); tên công ty trong quote còn rỗng (`stock_name:""`).
- **Cảnh báo collision**: mã US 3 chữ (IBM/BAC) bị nhận là VN khi `openstock_enabled` — chấp nhận trong deployment VN-only.

---

## Cách verify nhanh
```
curl http://127.0.0.1:8000/api/v1/health
curl "http://127.0.0.1:8000/api/v1/stocks/search?q=FPT"     # live OpenStock
curl "http://127.0.0.1:8000/api/v1/stocks/FPT/quote"        # qua adapter
```
Dashboard: http://127.0.0.1:5174 (nút 🌐 cycle 中/EN/VI). Phân tích AI: chọn mã VN → Reanalyze ở chế độ English để báo cáo ra tiếng Anh (báo cáo cũ giữ ngôn ngữ lúc tạo).

## Tài liệu tham chiếu (không lặp lại nội dung)
- `plan/daily_stock_analysis_adapter.md` — phân tích & kế hoạch adapter gốc.
- `plan/openstock_adapter_gaps.md` — trạng thái adapter + gaps.
- `plan/i18n_tracking.md` — audit i18n + bảng tracking + ưu tiên.
- Memory: `openstock-adapter.md`.

## Lưu ý bảo mật
- `.env` chứa `OPENAI_API_KEY` thật (đã **redact** khỏi tài liệu này). Không commit `.env`.

## Suggested skills
- `/run` — khởi động lại app (backend + dsa-web) đầu mỗi phiên.
- `/code-review` hoặc `/simplify` — khi bắt tay sửa P0/P1 i18n (review diff).
- `/verify` — xác nhận thay đổi i18n/adapter hoạt động trên app thật.
- Khi làm việc liên quan model/giá OpenAI/LLM → tham chiếu skill `claude-api` (chỉ cho Claude/Anthropic) hoặc tra web cho model OpenAI mới.
